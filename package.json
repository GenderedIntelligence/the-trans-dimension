{
  "name": "the-trans-dimension",
  "scripts": {
    "postinstall": "elm-tooling install",
    "start": "elm-pages dev --port 3000",
    "build": "elm-pages build",
    "format": "elm-format src",
    "test": "elm-test-rs"
  },
  "devDependencies": {
    "elm-optimize-level-2": "0.2.3",
    "elm-pages": "2.1.10",
    "elm-review": "^2.5.5",
    "elm-tooling": "^1.4.0"
  }
}
